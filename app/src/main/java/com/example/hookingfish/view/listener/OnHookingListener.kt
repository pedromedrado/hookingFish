package com.example.motivation1.view.listener

import android.content.Intent
import com.example.motivation1.view.MainActivity

interface OnGuestListener {

    fun onClick(id: Int)
    fun onDelete(id: Int)

}